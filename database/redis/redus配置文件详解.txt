# redis配置文件详解
################################## MODULES #####################################
# 在启动时加载一些模块
# loadmodule /path/to/my_module.so
# loadmodule /path/to/other_module.so

################################## NETWORK #####################################
# 指定能够访问本机的网络地址，使用ipv4和ipv6
# 默认是 bind 127.0.0.1
bind 127.0.0.1